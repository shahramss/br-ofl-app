name: product_specs_app
version: 1.1.0+2
publish_to: 'none'
description: مدیریت هوشمند ثبت مشخصات فنی محصولات ووکامرس با ورود وردپرس، ویس و هوش مصنوعی

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  flutter_secure_storage: ^9.2.2
  shared_preferences: ^2.2.3
  speech_to_text: ^7.4.0
  local_auth: ^2.3.0
  url_launcher: ^6.3.1

flutter:
  uses-material-design: true
  assets:
    - assets/app_icon/source_icon.png
